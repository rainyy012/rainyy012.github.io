name: Ping

# This is a manually triggered workflow to check if the custom runner is online

on:
  workflow_dispatch:

jobs:
  ping:
    runs-on: custom
    steps:
      - name: Ping
        run: echo "Hello"
